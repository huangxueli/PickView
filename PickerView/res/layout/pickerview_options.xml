<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:wheelview="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical" >

    <include
        android:layout_width="match_parent"
        android:layout_height="@dimen/pickerview_topbar_height"
        layout="@layout/include_pickerview_topbar" />

    <LinearLayout
        android:id="@+id/optionspicker"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/white"
        android:orientation="horizontal" >

        <!-- <com.lvfq.pickerview.lib.WheelView -->
        <!-- android:id="@+id/options_test" -->
        <!-- android:layout_width="match_parent" -->
        <!-- android:layout_height="wrap_content" -->
        <!-- android:layout_weight="1" -->
        <!-- wheelview:dividerColor="@android:color/white" -->
        <!-- wheelview:gravity="center" -->
        <!-- wheelview:textColorCenter="@android:color/primary_text_light" -->
        <!-- wheelview:textColorOut="@android:color/primary_text_light" -->
        <!-- wheelview:textSize="13sp"/> -->

        <com.lvfq.pickerview.lib.WheelView
            android:id="@+id/options1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1" />

        <com.lvfq.pickerview.lib.WheelView
            android:id="@+id/options2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1" />

        <com.lvfq.pickerview.lib.WheelView
            android:id="@+id/options3"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1" />
    </LinearLayout>

</LinearLayout>